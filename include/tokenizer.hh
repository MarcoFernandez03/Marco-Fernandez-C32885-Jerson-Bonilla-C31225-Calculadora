#include <string>
using namespace std;
#ifndef TOKENIZER_HH
#define TOKENIZER_HH
class tokenizer
{
    public:
    //PlaceHolder cambiar tipo por un stack/array de token una vez la clase Token esté implementada.
    int tokenize(string userExpression);
};
#endif